'use strict';

describe('the truth', () => {
  it('should be true', () => {
    let theTruth = true;
    expect(theTruth).toBe(true);
  });
});

//What I need to Mock is @nmq/q
//I need to make sure to mock the FS module and make sure those are being called correctly
